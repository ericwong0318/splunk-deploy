---
# Copy to my_splunk_secrets.yml (gitignored) and set a strong password.
# Prefer encrypting: ansible-vault encrypt my_splunk_secrets.yml
splunk_admin_password: "CHANGE_ME"
# Target host SSH user password and sudo/become password (often the same).
ansible_password: "CHANGE_ME"
ansible_become_password: "CHANGE_ME"
